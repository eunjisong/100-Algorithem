// Remove all non-alphanumeric characters (punctuation, spaces and symbols)

function palindrome(str) {
  return str.replace(/\W|_/g, "").toLowerCase()===str.replace(/\W|_/g,"").toLowerCase().split("").reverse().join("");
}
