// reverse string without using a loop 

function reverse(str){
return str.split("").reverse().join("");}

// reverse string with a loop. 

function reverse(str){
    var backward = "";
    for(i=str.length-1; i>-1; i--){
    backward += str[i];
    }
    return backward; 
}
